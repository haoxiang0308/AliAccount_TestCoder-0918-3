name: network_data_saver
description: A Dart application that fetches network data and saves it to randomly named .dart files.
version: 1.0.0

environment:
  sdk: ^3.0.0

dependencies:
  http: ^1.1.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0