#!/usr/bin/env julia

# Define a sample array
arr = [1, 2, 3, 4, 5]

# Calculate the sum
arr_sum = sum(arr)
println("Sum of the array: ", arr_sum)

# Calculate the mean
arr_mean = mean(arr)
println("Mean of the array: ", arr_mean)
