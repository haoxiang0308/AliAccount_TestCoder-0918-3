Nothing should be ignored in this case. The added files are VBA source files (.vba), which are source code files and should be tracked in version control. There are no build artifacts, dependencies, or temporary files in the change list that would warrant adding ignore rules.