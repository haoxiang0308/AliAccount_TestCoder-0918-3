Nothing should be ignored - the added files are both .groovy source files, which are legitimate source code files that should be tracked in version control. No build artifacts, dependencies, or temporary files are present in the changes.