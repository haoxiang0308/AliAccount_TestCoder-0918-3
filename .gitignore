```powershell
# PowerShell modules and profiles
Modules/
Profile.ps1
Microsoft.PowerShell_profile.ps1

# Environment and temp files
.env
.env.local
*.tmp
*.log

# System files
Thumbs.db
.DS_Store
```

Wait, let me reconsider this. The added files are just PowerShell and shell scripts (`get_processes.ps1`, `get_processes.sh`) which are source code files. There are no build artifacts, dependencies, or temporary files in the change list that need to be ignored.

(empty — output nothing)