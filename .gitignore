```
# JSON response files (if they are generated/api responses)
*.json
!*.json  # Keep if they are source/config files
```

Wait, I need to reconsider. `api_response.json` could be a generated file, but JSON files are often source/config files too. Since it's specifically named "api_response" it's likely a generated/response file, but I should be cautious about ignoring all JSON files.

Actually, looking at this more carefully - if this is just a single API response file and not part of a pattern of build artifacts or generated files, it might be source content. Without more context of other generated files, I'll treat it as potentially source content.

```
api_response.json
```