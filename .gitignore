terraform_config.hcl is a source configuration file, not a build artifact, dependency, or temporary file. No files in the change list require ignoring based on the rules provided.

(empty — output nothing)