# Метод для вычисления факториала числа
def factorial(n)
  # Проверка на неотрицательное число
  return 1 if n < 0
  # Вычисление факториала с помощью reduce (аналог inject)
  (1..n).reduce(1, :*)
end

# Пример использования
puts factorial(5) # Выведет 120