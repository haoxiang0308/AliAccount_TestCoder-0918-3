#include <gtest/gtest.h>
#include "utils.h"

TEST(UtilsTest, PrintMessageTest) {
    // This is a simple test that just verifies compilation
    // In a real project, you might test actual functionality
    EXPECT_TRUE(true);
}