// This is a placeholder Jest test file for MyButton.jsx.
// The required testing libraries could not be installed due to timeout.
// In a real scenario, you would install Jest and @testing-library/react,
// and the test would look something like the example below.

// import React from 'react';
// import { render, screen, fireEvent } from '@testing-library/react';
// import '@testing-library/jest-dom';
// import MyButton from './MyButton';

// describe('MyButton Component', () => {
//   test('renders correctly with label', () => {
//     render(<MyButton label="Click Me" />);
//     expect(screen.getByText(/click me/i)).toBeInTheDocument();
//   });

//   test('calls onClick handler when clicked', () => {
//     const mockOnClick = jest.fn();
//     render(<MyButton label="Submit" onClick={mockOnClick} />);
//     fireEvent.click(screen.getByRole('button', { name: /submit/i }));
//     expect(mockOnClick).toHaveBeenCalledTimes(1);
//   });

//   test('is disabled when disabled prop is true', () => {
//     render(<MyButton label="Disabled" disabled={true} />);
//     expect(screen.getByRole('button', { name: /disabled/i })).toBeDisabled();
//   });
// });

// For now, this file exists as a stub.